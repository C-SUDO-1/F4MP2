# CommonLibF4 address provider seam

This seam models resolving family-specific hook addresses from a provider instead of injecting raw success/failure plans. It is currently used by the player hook attempt stub.
