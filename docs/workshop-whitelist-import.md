# Workshop Whitelist Import

Use `tools/import_workshop_whitelist.py` to convert a simple TSV export into the runtime JSON whitelist format.

This allows replacing placeholder workshop form IDs without editing C/C++ source.
