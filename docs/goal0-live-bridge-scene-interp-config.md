# Goal 0 live bridge scene interpolation config

This iteration lets `MovementLiveClientBridge` optionally push interpolation config into the FO4 scene proxy backend stub.

Purpose:
- keep Goal 0 movement focused
- make backend presentation tunable from the live bridge layer
- preserve the separation between network target state and presented scene state
