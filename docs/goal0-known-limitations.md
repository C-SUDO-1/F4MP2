# Goal 0 known limitations

Known limitations at the Goal 0 lock point:

- local observation is still scaffolded rather than attached to a real FO4 hook site
- proxy actor presentation is still a driver stub rather than an in-engine actor controller
- workshop, dialogue, quest, and combat are intentionally out of scope for Goal 0 validation
- the older `movement_live_late_join_snapshot_smoke_test` remains weaker and less representative than the newer real-seam late-join test
