# Hook attempt executor

This layer turns a bring-up mode and a per-family install plan into structured `HookAttemptResult` records, applies them to the registry, and exposes blocking information for startup policy.
