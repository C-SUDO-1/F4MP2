# Hook Bring-up Policy

This policy layer reports whether the current installed hook families are sufficient for:

- basic sync: player + workshop
- vanilla mirror: player + actor + workshop + dialogue/quest

It is intentionally conservative and driven by the hook install registry.
